services:
  archibot:
    image: ${ARCHIBOT_IMAGE:-ghcr.io/mfkuh/archipelagodiscordbot:latest}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: archibot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
